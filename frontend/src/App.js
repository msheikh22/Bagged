import React from 'react';
import BuyerDashboard from './BuyerDashboard';

const App = () => {
  const userId = 'user123'; // Replace with login logic later
  return <BuyerDashboard userId={userId} />;
};

export default App;
